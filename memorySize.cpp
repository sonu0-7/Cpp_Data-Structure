#include <bits/stdc++.h>
using namespace std;

int main()
{
    // You can check the memory size of int, long, bool, long long, float, double and so on.
    // Only need to change the dataTypes of memorySize variable and see the results.
    int memorySize;
    cout << sizeof(memorySize) << " Bytes" << endl;

    return 0;
}