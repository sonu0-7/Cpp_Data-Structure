#include <iostream>
#include <vector>
using namespace std;

int main()
{
    // vector<int> custom_vector;
    // vector<int> custom_vector(size);
    // vector<int> custom_vector(size, initialValue);
    // vector<vector<int> > custom_vector (row, vector<int> (col, 8));


    // eg. 
    // vector<vector<int> > custom_vector;
    
    // vector<int> a {1,2,3}; // custom_vector[0].size() output: 3 ele
    // vector<int> b {4,5,6}; // // custom_vector[0].size() output: 3 ele
    // vector<int> b {4,5,6,9,11,13}; // should to use custom_vector[index].size(); output: 6 ele
    // vector<int> c {7,8,9}; // // custom_vector[0].size() output: 3 ele

    // custom_vector.push_back(a);
    // custom_vector.push_back(b);
    // custom_vector.push_back(c);

    // custom_vector.size(); // it's return the no's of rows of this vector
    // custom_vector[0].size() // it's return the no's of cols at 0 index.

    return 0;
}